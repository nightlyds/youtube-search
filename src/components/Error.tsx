import React from "react";

const Error: React.FC = () => {
    return (
        <div className="error">
            <span className="error-message">You caught the error :(</span>
        </div>
    );
};

export default Error;
